//show all pools and select one to add readings

import 'package:flutter/material.dart';

class Pools extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
